node_modules/
dist/
out/
*.vsix
*.log
.DS_Store
Thumbs.db
.vscode-test/
coverage/
.registry.json
.lightcode/
*.tsbuildinfo
.ripgrep-cache/

# Packed npm tarballs. They are build output, and a stale one checked in is a
# publishable artifact sitting several versions behind — `npm publish <tarball>`
# would ship it. One was committed by accident and survived eight releases.
*.tgz

# PowerPoint owner files. Office creates these beside an open document and deletes them on
# close, so they are never content — two were committed by a sweeping `git add -A`.
~$*
